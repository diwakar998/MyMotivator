name: Keep Streamlit Alive

on:
  schedule:
    - cron: "*/25 * * * *"  # every 25 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send request to keep app awake
        run: |
          curl -I https://mymotivator.streamlit.app/

